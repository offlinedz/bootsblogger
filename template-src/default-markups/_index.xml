<b:defaultmarkups>
  <b:comment>=== BlogArchive ===</b:comment>
  <!--(bake BlogArchive.xml)-->
  <b:comment>=== BlogSearch ===</b:comment>
  <!--(bake BlogSearch.xml)-->
  <b:comment>=== ContactForm ===</b:comment>
  <!--(bake ContactForm.xml)-->
  <b:comment>=== FeaturedPost ===</b:comment>
  <!--(bake FeaturedPost.xml)-->
  <b:comment>=== FollowByEmail ===</b:comment>
  <!--(bake FollowByEmail.xml)-->
  <b:comment>=== Header ===</b:comment>
  <!--(bake Header.xml)-->
  <b:comment>=== Image ===</b:comment>
  <!--(bake Image.xml)-->
  <b:comment>=== Label ===</b:comment>
  <!--(bake Label.xml)-->
  <b:comment>=== PopularPosts ===</b:comment>
  <!--(bake PopularPosts.xml)-->
  <b:comment>=== Translate ===</b:comment>
  <!--(bake Translate.xml)-->
</b:defaultmarkups>