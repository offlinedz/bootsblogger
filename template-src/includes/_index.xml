<b:comment>
-------------------------------------------------
Head
------------------------------------------------- </b:comment>

<b:defaultmarkups>
  <b:defaultmarkup type='Common'>
    <b:includable id='head.requiredMetaTags'>
      <!--(bake head/head.requiredMetaTags.xml)-->
    </b:includable>
    <b:includable id='head.title'>
      <!--(bake head/head.title.xml)-->
    </b:includable>
    <b:includable id='head.allHeadContent'>
      <!--(bake head/head.allHeadContent.xml)-->
    </b:includable>
    <b:includable id='head.defaultTags'>
      <!--(bake head/head.defaultTags.xml)-->
    </b:includable>
    <b:includable id='head.facebookTags'>
      <!--(bake head/head.facebookTags.xml)-->
    </b:includable>
    <b:includable id='head.twitterTags'>
      <!--(bake head/head.twitterTags.xml)-->
    </b:includable>
    <b:includable id='head.layoutModeCss'>
      <b:template-skin>
      <![CDATA[
      <!--(bake head/head.layout-mode.css)-->
      ]]>
      </b:template-skin>
    </b:includable>
  </b:defaultmarkup>
</b:defaultmarkups>

<b:comment>
-------------------------------------------------
Blog `<widget type='Blog'/>`
------------------------------------------------- </b:comment>

<b:defaultmarkups>
  <b:defaultmarkup type='Common'>
    <b:includable id='main.blog-breadcrumb' var='post'>
      <!--(bake blog-posts/main.blog-breadcrumb.xml)-->
    </b:includable>
    <b:includable id='main.blog-alert' var='post'>
      <!--(bake blog-posts/main.blog-alert.xml)-->
    </b:includable>
    <b:includable id='main.blog-posts' var='post'>
      <!--(bake blog-posts/main.blog-posts.xml)-->
    </b:includable>
    <b:includable id='main.blog-comments' var='post'>
      <!--(bake blog-posts/main.blog-comments.xml)-->
    </b:includable>
    <b:includable id='main.blog-pager' var='post'>
      <!--(bake blog-posts/main.blog-pager.xml)-->
    </b:includable>
    <b:includable id='byline.header'>
      <!--(bake blog-posts/byline.header.xml)-->
    </b:includable>
    <b:includable id='byline.footer'>
      <!--(bake blog-posts/byline.footer.xml)-->
    </b:includable>
    <b:includable id='post.authorName'>
      <!--(bake blog-posts/post.authorName.xml)-->
    </b:includable>
    <b:includable id='post.authorImage'>
      <!--(bake blog-posts/post.authorImage.xml)-->
    </b:includable>
    <b:includable id='post.datePublished'>
      <!--(bake blog-posts/post.datePublished.xml)-->
    </b:includable>
    <b:includable id='post.dateUpdated'>
      <!--(bake blog-posts/post.dateUpdated.xml)-->
    </b:includable>
    <b:includable id='post.numComments'>
      <!--(bake blog-posts/post.numComments.xml)-->
    </b:includable>
    <b:includable id='post.labels'>
      <!--(bake blog-posts/post.labels.xml)-->
    </b:includable>
    <b:includable id='post.featuredImage'>
      <!--(bake blog-posts/post.featuredImage.xml)-->
    </b:includable>
    <b:includable id='post.featuredImageBg'>
      <!--(bake blog-posts/post.featuredImageBg.xml)-->
    </b:includable>
    <b:includable id='post.snippet'>
      <!--(bake blog-posts/post.snippet.xml)-->
    </b:includable>
    <b:includable id='post.jumpLink'>
      <!--(bake blog-posts/post.jumpLink.xml)-->
    </b:includable>
  </b:defaultmarkup>
</b:defaultmarkups>

<b:comment>=== Comments JS ===</b:comment>
<b:if cond='data:view.isSingleItem'>
<script>
//<![CDATA[
(function ($) {
  'use strict';

  $(function () {

    // Toggle `.active` for `.comment-form-original-wrapper` and `.comment-form-reply-wrapper`

    $('.comment-form-original-wrapper').addClass('active')

    $('.js-comment-action').on('click', function () {
      $('.comment-form-original-wrapper, .comment-form-reply-wrapper').each(function () {
        if ($(this).find('.comment-form').length) {
          $(this).addClass('active')
        } else {
          $(this).removeClass('active')
        }
      })
    })


    // Toggle `.show` for `.comment-add-wrapper` and `.comment-cancel-wrapper`

    $('.js-comment-action').on('click', function () {
      $('.js-comment-action').removeClass('active')
      $(this).addClass('active')

      $('.comment-form-original-wrapper').each(function () {
        if ($(this).find('.comment-form').length) {
          $('body').find('.comment-add-wrapper').removeClass('show')
        } else {
          $('body').find('.comment-add-wrapper').addClass('show')
        }
      })
      $('.comment-form-reply-wrapper').each(function () {
        if ($(this).find('.comment-form').length) {
          $('body').find('.comment-cancel-wrapper').addClass('show')
        } else {
          $('body').find('.comment-cancel-wrapper:not(.show)').removeClass('show')
        }
      })
    })


    // Loading animation

    $('.comment-form').addClass('comment-form-loading-wrapper')

    $('#comment-editor').on('load', function () {
      $('.comment-form').removeClass('comment-form-loading-wrapper')
    })

    $('.js-comment-action').click(function () {
      $('.comment-form').addClass('comment-form-loading-wrapper')

      $('#comment-editor').on('load', function () {
        $('.comment-form').removeClass('comment-form-loading-wrapper')
      })
    })

  })

}(jQuery));
//]]>
</script>
</b:if>

<b:comment>
Unused default inclusions
  Default inclusions can not be deleted.
  But, you can close an inclusion (e.g., `<b:includable id='foo'/>`).
</b:comment>

<b:defaultmarkups>
  <b:defaultmarkup type='Blog'>
    <b:includable id='aboutPostAuthor'/>
    <b:includable id='addComments'/>
    <b:includable id='commentAuthorAvatar'/>
    <b:includable id='commentDeleteIcon'/>
    <b:includable id='commentForm'/>
    <b:includable id='commentFormIframeSrc'/>
    <b:includable id='commentItem'/>
    <b:includable id='commentList'/>
    <b:includable id='commentPicker'/>
    <b:includable id='comments'/>
    <b:includable id='commentsTitle'/>
    <b:includable id='feedLinks'/>
    <b:includable id='feedLinksBody'/>
    <b:includable id='homePageLink'/>
    <b:includable id='iframeComments'/>
    <b:includable id='inlineAd'/>
    <b:includable id='nextPageLink'/>
    <b:includable id='post'/>
    <b:includable id='postBody'/>
    <b:includable id='postBodySnippet'/>
    <b:includable id='postCommentsAndAd'/>
    <b:includable id='postCommentsLink'/>
    <b:includable id='postFooter'/>
    <b:includable id='postFooterAuthorProfile'/>
    <b:includable id='postHeader'/>
    <b:includable id='postJumpLink'/>
    <b:includable id='postMeta'/>
    <b:includable id='postPagination'/>
    <b:includable id='postTitle'/>
    <b:includable id='previousPageLink'/>
    <b:includable id='status-message'/>
    <b:includable id='threadedCommentForm'/>
    <b:includable id='threadedCommentJs'/>
    <b:includable id='threadedComments'/>
  </b:defaultmarkup>
</b:defaultmarkups>

<b:comment>
-------------------------------------------------
Icons
------------------------------------------------- </b:comment>

<b:defaultmarkups>
  <b:defaultmarkup type='Common'>
    <b:includable id='icons.blogger'>
      <!--(bake assets/icons/icons.blogger.xml)-->
    </b:includable>
  </b:defaultmarkup>
  <b:includable id='icons.bootsblogger'>
    <!--(bake assets/icons/icons.bootsblogger.xml)-->
  </b:includable>
</b:defaultmarkups>

<b:comment>
-------------------------------------------------
Assets
------------------------------------------------- </b:comment>

<b:defaultmarkups>
  <b:defaultmarkup type='Common'>
    <b:includable id='css.bootstrap'>
      <style>
      <!--(bake assets/css/bootstrap/bootstrap.min.css)-->
      </style>
    </b:includable>
    <b:includable id='css.bootsblogger'>
      <style>
      <!--(bake assets/css/bootsblogger/bootsblogger.min.css)-->
      </style>
    </b:includable>
    <b:includable id='css.fontAwesome'>
      <link crossorigin='anonymous' href='{{ blog.cdn.fontAwesome }}' integrity='{{ blog.cdn.fontAwesome_hash }}' rel='stylesheet'/>
    </b:includable>
    <b:includable id='js.jquery'>
      <script crossorigin='anonymous' integrity='{{ blog.cdn.jquery_hash }}' src='{{ blog.cdn.jquery }}'/>
    </b:includable>
    <b:includable id='js.popper'>
      <script crossorigin='anonymous' integrity='{{ blog.cdn.popper_hash }}' src='{{ blog.cdn.popper }}'/>
    </b:includable>
    <b:includable id='js.bootstrap'>
      <script crossorigin='anonymous' integrity='{{ blog.cdn.bootstrapJs_hash }}' src='{{ blog.cdn.bootstrapJs }}'/>
    </b:includable>
  </b:defaultmarkup>
</b:defaultmarkups>